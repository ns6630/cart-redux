import styled from "styled-components";
import icon from "../img/clear.svg";
import Icon from "./Icon";

const ClearIcon = styled(Icon).attrs({
  src: icon,
  alt: "Clear",
})``;

export default ClearIcon;