import styled from "styled-components";
import icon from "../../img/minus.svg";
import Icon from "./Icon";

const RemoveIcon = styled(Icon).attrs({
  src: icon,
  alt: "Remove",
})``;

export default RemoveIcon;